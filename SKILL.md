---
name: photo-toolbox
description: 照片处理工具箱，主打证件照排版，共四大功能：(1) 将人像照自动裁切成证件照规格（一寸/二寸/大一寸）并按 5 寸/6 寸相纸排版成一张可打印图，在家打证件照；(2) 将多张照片按网格整齐排列导出 PDF；(3) 将多张照片排列到 Word 文档且每张照片下方预留备注/价格填写空间；(4) 给照片一键批量加文字水印（角标/居中/平铺）用于宣传发布和版权保护。用户首次使用时 Agent 会发起一连串引导问题收集默认排版与水印偏好并保存到 user-preferences.json，后续按偏好执行。当用户发送照片并要求"证件照""一寸""二寸""办证照片""打印证件照""每页放 X 张""照片排版""排成 PDF""排成 Word""照片下面留空写价格/备注""加水印""打 logo""防盗图""样品水印""第一次用/设置默认偏好"等需求时使用。支持手机端和电脑端，用户直接发照片即可。自动识别照片方向（超竖/竖/正方/横版）并同类分组排版。
---

# 照片处理工具箱

四个独立功能，按用户需求选择对应脚本（证件照是主打功能）：

| 功能 | 脚本 | 输出 | 典型场景 |
|------|------|------|---------|
| 照片排版 PDF | `scripts/layout_photos.py` | .pdf | 照片整理、分享、存档 |
| 照片排版 Word（带备注） | `scripts/layout_photos_word.py` | .docx | 报价单、商品图册、照片说明 |
| 照片加水印 | `scripts/add_watermark.py` | .jpg | 宣传发布、版权保护、样品标注 |
| 证件照排版 | `scripts/layout_id_photo.py` | .png | 一寸/二寸证件照自动裁切并按相纸排版打印 |

**先判断用户要哪个功能**：说"PDF"用功能一；说"Word/下面打字/写价格/备注"用功能二；说"水印/防盗/样品/版权"用功能三；说"证件照/一寸/二寸/办证照片/打印照片"用功能四。不确定时直接问一句。

---

## 首次使用引导（Onboarding，必读）

> 本技能按每个用户的**偏好配置**执行。用户在第一次使用前，Agent 要主动发起一连串引导问题收集偏好，并保存到 skill 目录下的 `user-preferences.json`。没有配置就执行的，只允许出现在用户明确说"就用默认"之后。

### 触发条件

检测 skill 目录下**不存在** `user-preferences.json` 时，判定为首次使用。用户在第一次提出照片处理需求时，**先发起引导，再执行任务**。

### 引导流程（Agent 逐组询问，每组问题简短、一次问完）

**第 1 组 · PDF 排版偏好**
1. 每页放几张照片？（默认 9）
2. 纸张大小用哪个？（A4 / Letter / A3，默认 A4）
3. 照片要按横竖方向分组排列吗？（默认分组；如不希望按方向归类选"不分组"）

**第 2 组 · Word 排版偏好**
1. 每页放几张照片？（默认 6）
2. 每张照片下方留几行空行，方便写价格/备注？（默认 2 行）

**第 3 组 · 水印偏好**
1. 默认需要加水印吗？（默认不需要）
2. 如果需要：水印文字写什么？（如品牌名、作品名）
3. 放在什么位置？（正中偏下 / 右下角 / 正中央 / 平铺防盗，默认正中偏下）
4. 要不要斜着放？（默认不斜；如需要给出角度，如 20°）
5. 颜色/透明度等其他样式用默认即可（白色半透明带深色描边），需要调整再说明。

**第 4 组 · 证件照偏好**
1. 打证件照时默认用什么规格？（一寸 / 二寸 / 大一寸，默认一寸）
2. 默认用哪种相纸打印？（5 寸 / 6 寸，默认 5 寸）

**每一组用户都可以回答"跳过/默认"**——跳过项使用下面的默认值，不要反复追问。引导控制在 1-2 轮内完成，不要打断用户的原始需求太久。

### 保存偏好

把答案合并到 skill 目录下的 `user-preferences.json`（格式见同目录 `user-preferences.json.example`），未作答的字段用默认值补齐。保存后告知用户："已保存你的默认设置：PDF 每页 9 张、A4、方向分组；Word 每页 6 张、留 2 行备注；水印默认关闭。以后需要修改随时说'重新设置照片偏好'。"

### 后续执行规则

1. 每次执行前先读取 `user-preferences.json`。
2. 用户**本次明确指定**的参数 > 偏好配置 > 脚本默认值。
3. 用户本次没有提到排版/水印细节时，直接用偏好配置执行，不要重复问"每页几张"这类已配置过的问题。
4. 用户说"重新设置照片偏好 / 改一下默认设置 / 重新引导"时，重新发起上述引导并覆盖保存。

### 偏好文件字段说明

| 字段 | 含义 | 默认 |
|------|------|------|
| pdf.per_page | PDF 每页张数 | 9 |
| pdf.page_size | 纸张 a4/letter/a3/4r 或 宽x高mm | a4 |
| pdf.fit | contain 完整 / cover 裁剪 | contain |
| pdf.group_by_orientation | 是否方向分组 | true |
| pdf.dpi | 嵌入分辨率 | 200 |
| word.per_page | Word 每页张数 | 6 |
| word.note_lines | 每图下方备注行数 | 2 |
| watermark.enabled | 默认是否加水印 | false |
| watermark.text | 水印文字 | 空 |
| watermark.position | 位置 center-low/center/bottom-right/…/tile | center-low |
| watermark.angle | 旋转角度（0=不斜） | 0 |
| watermark.opacity | 不透明度 0-255 | 128 |
| watermark.color | 颜色 R,G,B | 255,255,255 |
| watermark.format | jpeg/png | jpeg |
| watermark.max_size | 输出长边上限，0=不缩放 | 4000 |
| id_photo.spec | 证件照规格 | 1寸 |
| id_photo.paper | 打印相纸 | 5寸 |

---

## 功能一：照片排版 PDF

将多张照片按指定每页张数，自动计算网格（9 张→3×3、6 张→2×3），整齐排列到 A4 等页面导出 PDF。照片保持原始比例，居中，自动处理 EXIF 旋转。

### 使用流程

1. **接收照片**：用户直接发送多张照片（手机相册多选/电脑拖拽），或提供文件夹路径。支持 jpg/jpeg/png/webp/heic/bmp/tiff。
2. **确认参数**（未说明用 `user-preferences.json` 的偏好值，本次明确指定 > 偏好 > 默认；不要重复问已配置的问题）：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 每页张数 | 9 | 可任意指定 |
| 页面尺寸 | A4 | a4/letter/a3/a5/4r 或 宽x高mm |
| DPI | 200 | 屏幕 150，打印 300，越大文件越大 |
| 图片适配 | contain | contain=完整显示可能留白，cover=裁剪填满 |
| 边距/间距 | 16pt / 3pt | |
| 方向分组 | 开启 | 同类方向聚在一起 |

3. **调用脚本**：
```bash
python3 <skill_dir>/scripts/layout_photos.py <图片...> --output <输出.pdf> --per-page 9 --dpi 200
```

4. **验证交付**：检查 PDF 非空、页数=ceil(总数/每页张数)、大小合理（200DPI 每页约 0.5-2MB），通过 `present_files` 交付。

### PDF 脚本完整参数
```
-n, --per-page N       每页照片数（默认 9）
--rows N --cols N      手动指定网格行列
--page-size SIZE       a4/letter/a3/a5/4r 或 宽x高mm（默认 a4）
--margin PT            页边距（默认 16）
--gap PT               照片间距（默认 3）
--dpi N                嵌入分辨率（默认 200；屏幕150，打印300）
--fit MODE             contain 留白 / cover 裁剪（默认 contain）
--quality N            JPEG 质量 1-100（默认 85）
--sort MODE            name/date/none（默认 name）
--no-group-by-orientation  关闭方向分组
```

### DPI 选择
| 用途 | DPI | 9张/页预计大小 |
|------|-----|---------------|
| 手机查看、微信发送 | 150 | 0.5-1.5 MB |
| 电脑查看、一般分享 | 200 | 1-3 MB |
| 打印、存档 | 300 | 3-8 MB |

文件太大时优先降 DPI 到 150，而不是降 JPEG 质量。

---

## 功能二：照片排版 Word（每张图下预留备注）

将多张照片排列到 Word A4 页面，**每张照片下方自动留出带下划线的空白行，供用户输入价格或备注**。默认每页 6 张。适用于商品报价单、照片说明、盘点表。

### 使用流程

1. **接收照片**（同功能一）。
2. **确认参数**（未说明用 `user-preferences.json` 的偏好值）：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 每页张数 | 6 | Word 默认 6 张（2×3 或 3×2） |
| 备注行数 | 2 | 每张照片下方留几行可输入横线 |
| 照片宽度 | 5.5cm | |
| 页面边距 | 1.5cm | |

3. **调用脚本**：
```bash
python3 <skill_dir>/scripts/layout_photos_word.py <图片...> --output <输出.docx> --per-page 6 --note-lines 2
```

4. **验证交付**：检查 docx 非空、表格网格正确、每张图下有对应数量的备注下划线行，通过 `present_files` 交付。用户打开后可直接在横线上打字。

### Word 脚本完整参数
```
-n, --per-page N       每页照片数（默认 6）
--rows N --cols N      手动指定网格行列
--note-lines N         每张照片下方备注行数（默认 2）
--photo-width CM       照片宽度厘米（默认 5.5）
--margin CM            页面边距厘米（默认 1.5）
--sort MODE            name/date/none（默认 name）
```

**依赖**：需要 python-docx，缺失时先运行 `pip install python-docx`。

---

## 功能三：照片一键加水印

给一张或一批照片加文字水印，输出到独立文件夹（不覆盖原图）。支持角标、居中、平铺三种模式，用于宣传发布和版权保护。

### 使用流程

1. **接收照片** + **确认水印要求**：先读取偏好（watermark.enabled/text/position 等）；用户本次没说但偏好已开启时用偏好水印；偏好未开启且用户没说时，确认是否要加水印、文字是什么（如品牌名、"样品"、"© 公司名"）。
2. **选择模式**（根据用户描述或偏好判断）：

| 用户说法 | 模式 | --position |
|---------|------|-----------|
| "正中间偏下加个水印/默认位置" | 正中偏下（默认） | center-low |
| "右下角加个品牌名/署名" | 右下角角标 | bottom-right |
| "正中央盖个章/标机密" | 居中大字 | center |
| "防盗图/铺满水印/样品图" | 斜向平铺 | tile |
| 其他角 | 四角 | top-left/top-right/bottom-left |
| "斜着放/旋转" | 任意位置 + 旋转角度 | 任意 + --angle N |

3. **调用脚本**：
```bash
python3 <skill_dir>/scripts/add_watermark.py <图片...> --text "水印文字" --position bottom-right --output-dir <输出文件夹>
```

4. **验证交付**：检查输出文件夹中每张图都有水印、原图未被修改，将加水印后的图片（或打包）通过 `present_files` 交付。

### 水印脚本完整参数
```
-t, --text TEXT        水印文字（必填）
-o, --output-dir DIR   输出文件夹（默认 ./watermarked，不覆盖原图）
--position MODE        center-low(默认，正中偏下)/center/bottom-right/bottom-left/top-right/top-left/tile(平铺)
--opacity N            不透明度 0-255（默认 128 半透明；平铺建议 60-80）
--font-size N          字号像素（默认按图片宽度 5% 自适应）
--color R,G,B          文字颜色（默认白色 255,255,255）
--angle N              旋转角度（度），角标/居中模式指定即斜放；平铺模式默认 30
--max-size N           输出长边上限像素（默认 4000，超大图等比缩小提速；0=不缩放保持原分辨率）
--format jpeg/png      输出格式（默认 jpeg 质量 92；png 无损更清晰、文件更大）
--suffix TEXT          输出文件名后缀（默认 _wm）
--quality N            JPEG 质量（默认 92）
```

**要点**：角标/居中模式自动加深色描边，保证水印在浅色和深色背景上都看得清；中文字体自动适配 macOS/Windows/Linux。

---

## 功能四：证件照排版

把人像照片自动裁切成证件照规格（一寸/二寸等），并按相纸尺寸（5 寸/6 寸）排版成一张可打印图，在家就能打证件照。300dpi 打印标准。

### 使用流程

1. **接收人像照** + **确认规格**（用户没指定时用偏好配置，见「首次使用引导」）：
   - 证件照规格：一寸（25×35mm，默认）/ 二寸（35×49mm）/ 大一寸（33×48mm）/ 护照 / 自定义 `宽x高mm`
   - 相纸：5 寸（89×127mm，默认）/ 6 寸（102×152mm）/ A4 / 自定义
2. **调用脚本**：
```bash
python3 <skill_dir>/scripts/layout_id_photo.py <人像照> --spec 1寸 --paper 5寸 -o <输出.png>
```
3. **验证交付**：检查排版图行列数正确（一寸5寸=3×3=9张、二寸5寸=2×2=4张）、居中无错位，通过 `present_files` 交付，并告知用户打印前核对头部位置（必要时用 `--head-offset` 微调）。

### 证件照排版脚本完整参数
```
-s, --spec SPEC        证件照规格：1寸(默认)/2寸/大一寸/小一寸/护照 或 '宽x高mm'
-p, --paper PAPER      相纸：5寸(默认)/6寸/A4 或 '宽x高mm'
--dpi N                输出分辨率（打印默认 300）
--head-offset N        头部位置 0~1：0=裁切框顶部对齐原图顶部，0.12=头部靠上(默认)，0.5=居中
--gap MM               排版间距（默认 2mm）
--margin MM            相纸留白（默认 5mm）
--bg R,G,B             背景色（默认白色）
--single-dir DIR       同时输出单张证件照到该文件夹（可选）
```

### 证件照常用尺寸（mm，打印用 300dpi）
| 规格 | 尺寸 | 5寸相纸排版 | 6寸相纸排版 |
|------|------|-----------|-----------|
| 一寸 | 25×35 | 3×3=9 张 | 3×3=9 张 |
| 二寸 | 35×49 | 2×2=4 张 | 2×2=4 张 |
| 大一寸 | 33×48 | 2×2=4 张 | 2×2=4 张 |

**要点**：自动 EXIF 转正；默认头部靠上（`--head-offset 0.12`）符合证件照习惯；白底居中排列，可直接打印后裁剪。

---

## 方向自动分组（功能一、二通用，默认开启）

脚本自动识别每张照片宽高比，分四类，同类连续排列：

| 类别 | 宽高比 | 典型 |
|------|--------|------|
| 超竖版 | ≤ 0.6 | 9:16 手机竖拍 |
| 竖版 | 0.6~0.85 | 3:4、2:3 |
| 正方形 | 0.85~1.15 | 1:1 |
| 横版 | > 1.15 | 4:3、16:9 |

比例接近的照片在统一网格中显示大小一致，避免横版混在竖版中显得特别小。组间按数量降序，组内按文件名/时间排序。用户说"不要分类/按原顺序"时，PDF 加 `--no-group-by-orientation`。

### 自动网格规则
4 张→2×2，6 张→2×3 或 3×2，8 张→2×4，9 张→3×3，12 张→3×4（自动选最接近正方形的）。

---

## 异常处理

**接收阶段**：
- `user-preferences.json` 不存在 → 判定首次使用，先按「首次使用引导」章节发起偏好询问，再执行任务（用户明确说"用默认"则直接创建默认配置并继续）
- 0 张照片 → 告知"没有检测到照片，请重新发送"，不执行脚本
- 1 张照片 → 正常处理，PDF/Word 中居中不硬排网格；水印正常加
- 部分格式不支持/损坏 → 跳过并明确告知用户哪张被跳过，继续处理其余

**执行阶段**：
- 缺依赖（reportlab/Pillow/python-docx）→ `pip install` 对应库后重试
- 脚本报错 → 读错误信息，路径错误先检查输出目录是否存在
- PDF 生成后为 0 字节/打不开 → 删除后用更低 DPI 重生成一次，仍失败则告知用户
- 照片总数不能被每页张数整除 → 正常处理，最后一页留白，并告知"共 X 页，最后一页 Y 张"
- 证件照 `--spec/--paper` 无法识别 → 提示用"1寸/2寸/5寸/6寸"关键词或"宽x高mm"格式
- 证件照人像头部位置不对（裁掉头顶/太空）→ 用 `--head-offset` 微调（默认 0.12，越靠近 0 头部越靠上）

🔴 **CHECKPOINT**：
- 用户要求 cover 裁剪模式 → 先告知"会裁掉照片边缘，确定吗？"，确认后执行
- 照片数不能被每页张数整除 → 生成前告知分页结果（如 20 张÷9=2 页余 2 张），无异议再执行
- 水印 opacity 较高或居中大字可能遮挡主体 → 先出 1 张样图确认效果，再批量处理

---

## 反例与黑名单（不要做什么）

1. **不要在用户没要求时擅自用 cover 裁剪**——默认 contain 保持照片完整
2. **不要改变用户照片原始顺序**——除非用户明确要求按时间/文件名排序
3. **不要把 0 张或 1 张照片硬排 3×3 网格**
4. **不要把输出文件放在输入图片同一文件夹再重新扫描**——会把输出当输入
5. **不要在手机端只给链接不说明保存方法**——告知"全屏预览→分享→存储到文件"
6. **不要在分页不整除时不告知用户最后一页有几张**
7. **不要跳过损坏照片而不告知用户是哪张**
8. **不要在 DPI 300 时不提示文件会很大**
9. **加水印不要覆盖原图**——必须输出到独立文件夹（脚本默认行为，不要改成原地修改）
10. **Word 排版不要忘记留备注行**——这是功能二的核心，默认 --note-lines 2
11. **不要在用户只说"排版"时默认猜格式**——PDF 和 Word 用途不同，不确定就问一句
12. **不要在首次使用时跳过引导直接按默认执行**——除非用户明确说"就用默认/跳过"，否则先按「首次使用引导」章节发起偏好询问
13. **不要重复询问已配置过的偏好**——用户没再提时直接用 user-preferences.json 的偏好值，避免每次都要用户再说一遍每页几张
14. **不要把证件照排版当普通排版做**——证件照要按规格裁切比例并用到 `layout_id_photo.py`，不能用 layout_photos.py 直接排原始照片
15. **不要在证件照排版时擅自引入 AI 抠图**——本项目不内置人脸检测/抠图模型，遇到背景不纯的需求，先说明"本工具仅做比例裁切排版，不支持自动抠背景"，再确认是否继续

## 手机端使用说明

1. 用户点"+"从相册多选照片发送，说明需求（如"每页6张排成Word，下面留空写价格"或"给这些图加'样品'水印"）
2. AI 接收后调用对应脚本生成
3. PDF/Word/图片通过文件卡片发送，用户可预览、保存到"文件"App 或分享
4. 手机一次可选照片数受系统限制，很多时分批发送，AI 收集齐后统一处理

## 注意事项

- 自动处理 EXIF 旋转，手机竖图不会横过来
- PNG 透明通道自动合成白色背景
- 最后一页照片不足时从左到右、从上到下排列，剩余留白
- 排序默认按文件名；用户强调"拍摄顺序"时用 `--sort date`

## 持续改进

用户反馈、版本历史、测试用例和优化流程详见 `references/feedback-and-improvement.md` 和 `references/feedback-log.md`。积累 3 条以上反馈或遇到严重问题时，建议用户说「用达尔文优化 photo-toolbox」触发系统性优化。
