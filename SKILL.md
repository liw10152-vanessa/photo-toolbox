---
name: photo-layout-pdf
description: 将多张照片按网格整齐排列到 PDF 页面中并导出。当用户发送多张照片/图片并要求排版、拼图、整理成 PDF，或提到"每页放 X 张""照片排版""照片排成 PDF""图片整理成 PDF""把这些照片导出 PDF""照片网格排版"等需求时使用。支持手机端和电脑端，用户直接发照片即可，无需提供文件夹路径。自动识别照片方向（超竖/竖/正方/横版）并同类分组排版。
---

# 照片排版 PDF

将用户发送的多张照片按指定每页张数，自动计算网格（如 9 张→3×3、6 张→2×3），整齐排列到 A4 等页面中，导出为 PDF。照片保持原始比例，居中放置，自动处理 EXIF 旋转方向。自动识别照片方向并同类分组，避免横版混在竖版中显得特别小。

## 使用流程

### 1. 接收照片

用户会直接发送多张照片（手机端从相册选择，电脑端拖拽或上传）。这些照片会作为附件出现在对话中，AI 可以获取它们的本地路径。

- 如果用户发的是一个文件夹路径，也可以直接使用
- 收集所有图片格式：jpg、jpeg、png、webp、heic、bmp、tiff

### 2. 确认关键参数

向用户确认（或从用户描述中提取）以下参数，未说明的使用默认值：

| 参数 | 默认值 | 说明 |
|------|--------|------|
| 每页张数 | 9 | 用户说"每页9张"就用 9 |
| 页面尺寸 | A4 | 可选 a4/letter/a3/a5/4r 或自定义 宽x高mm |
| DPI | 200 | 屏幕查看 150 足够，打印用 300，越大文件越大 |
| 图片适配 | contain | contain=保持比例完整显示（可能留白），cover=裁剪填满 |
| 边距 | 16pt | 约 0.22 英寸 |
| 间距 | 3pt | 照片之间的空隙 |
| 方向分组 | 开启 | 自动识别超竖/竖/正方/横版，同类放在一起 |

**不需要问太多问题**，用户说"每页9张"就直接用默认值跑。只有当用户明确要求特定页面尺寸、打印质量或裁剪时才调整。

### 3. 调用脚本生成 PDF

使用 `scripts/layout_photos.py` 脚本。将用户上传的所有图片路径作为输入参数。

```bash
python3 <skill_dir>/scripts/layout_photos.py <图片1路径> <图片2路径> ... --output <输出PDF路径> --per-page <每页张数> --dpi <DPI>
```

也可以传入一个文件夹路径（脚本会自动收集文件夹内所有图片）：

```bash
python3 <skill_dir>/scripts/layout_photos.py <图片文件夹路径> --output <输出PDF路径> --per-page 9
```

**输出路径**建议放在当前工作目录下，文件名用有意义的名称，如 `照片排版_9张每页.pdf`。

### 4. 验证并交付

脚本运行成功后会输出页数、网格和文件大小。快速验证：
- 检查生成的 PDF 文件存在且非空
- 页数 = ceil(照片总数 / 每页张数)
- 文件大小合理（200 DPI 下每页约 0.5-2 MB，取决于照片内容）

验证通过后，通过 `present_files` 将 PDF 交付给用户。用户在手机端可以直接预览和保存到本地/文件 App。

## 脚本完整参数

```
python3 layout_photos.py <输入...> -o <输出.pdf> [选项]

输入: 图片文件路径或包含图片的文件夹，可多个

选项:
  -n, --per-page N    每页照片数量（默认 9）
  --rows N --cols N   手动指定网格行列（不指定则自动计算最接近正方形的网格）
  --page-size SIZE    页面尺寸：a4/letter/a3/a5/4r 或 宽x高mm（默认 a4）
  --margin PT         页边距（默认 16）
  --gap PT            照片间距（默认 3）
  --dpi N             图片嵌入分辨率 DPI（默认 200；屏幕150，打印300）
  --fit MODE          contain=保持比例留白 / cover=裁剪填满（默认 contain）
  --quality N         JPEG 压缩质量 1-100（默认 85）
  --sort MODE         name=按文件名 / date=按修改时间 / none=输入顺序（默认 name）
  --no-group-by-orientation  关闭方向自动分组（默认开启）
```

## 方向自动分组（默认开启）

脚本自动识别每张照片的宽高比，分为四类，同类连续排列：

| 类别 | 宽高比范围 | 典型比例 |
|------|-----------|---------|
| 超竖版 | ≤ 0.6 | 9:16、9:19.5（手机竖拍） |
| 竖版 | 0.6 ~ 0.85 | 3:4、2:3（相机竖拍） |
| 正方形 | 0.85 ~ 1.15 | 1:1 |
| 横版 | > 1.15 | 4:3、16:9（横拍） |

**作用**：比例接近的照片在统一网格中显示大小一致，避免横版混在竖版中显得特别小。组间按数量降序（多数派在前），组内按文件名或时间排序。

用户说"不要分类""按原顺序""不要分组"时，加 `--no-group-by-orientation` 参数关闭。

## 参数选择指南

### DPI 与文件大小的平衡

| 用途 | 推荐 DPI | 9 张/页 A4 预计大小 |
|------|---------|---------------------|
| 手机查看、微信发送 | 150 | 0.5-1.5 MB |
| 电脑查看、一般分享 | 200（默认） | 1-3 MB |
| 打印、高质量存档 | 300 | 3-8 MB |

用户抱怨文件太大时，优先降低 DPI 到 150，而不是降低 JPEG 质量。

### contain vs cover

- **contain（默认）**：照片完整显示，横竖混排时每张照片都不变形，格子内可能有白边。适合大多数场景。
- **cover**：照片裁剪填满整个格子，无白边，但会裁掉部分内容。适合所有照片都是同一方向且用户要求"铺满"的场景。

### 自动网格规则

不指定 rows/cols 时，脚本自动选择最接近正方形的网格：
- 4 张 → 2×2
- 6 张 → 2×3（横版页面）或 3×2
- 9 张 → 3×3
- 8 张 → 2×4
- 12 张 → 3×4

## 手机端使用说明

本 Skill 完全支持手机端豆包 App：
1. 用户在手机对话中点击"+"选择相册照片（可多选）
2. 发送照片并说明要求（如"每页9张，排成PDF"）
3. AI 接收照片后调用脚本生成 PDF
4. 生成的 PDF 通过文件卡片发送，用户可在手机上预览、保存到"文件"App 或分享

**注意**：手机端用户一次最多能选的照片数量受系统限制，照片很多时分批发送即可，AI 会收集所有附件后统一处理。

## 注意事项

- 脚本会自动处理照片的 EXIF 旋转信息，手机拍的竖图不会横过来
- PNG 等带透明通道的图片会自动合成白色背景
- 最后一页照片不足时，按顺序从左到右、从上到下排列，剩余空格留白
- 排序默认按文件名，用户如果强调"按拍摄顺序"，使用 `--sort date`
- 如果用户上传的照片中有损坏的文件，脚本会跳过并打印警告，不影响其他照片
- 不要把输出 PDF 放在输入图片同一个文件夹内再重新扫描，避免重复收集
- 方向分组默认开启，大多数手机竖拍照片会自动聚在一起，显示大小一致

## 反馈与持续改进

本 Skill 设计为可持续进化。每次使用后，如果用户提出问题、建议或不满，必须执行以下流程：

### 记录反馈

将用户反馈追加到 `references/feedback-log.md` 的反馈记录表中，包含：日期、来源、反馈内容、影响范围、状态（待处理）。不要只在对话里回应而不记录——反馈日志是持续改进的唯一依据。

### 触发优化

当出现以下任一情况时，主动建议用户触发 Darwin Skill 进行系统性优化：
- 反馈日志中积累了 3 条以上「待处理」反馈
- 用户报告了严重问题（如脚本报错、PDF 无法打开、照片方向错误）
- 用户明确说"优化一下这个 skill"或"改进一下"

触发方式：告诉用户说「**用达尔文优化 photo-layout-pdf**」即可。Darwin Skill 会自动读取本目录的 `test-prompts.json` 作为测试基准，执行评估→改进→验证循环。

### 优化后更新

Darwin 优化完成后：
1. 将反馈日志中对应反馈的状态改为「已采纳」，填写处理结果
2. 在 `references/feedback-log.md` 的版本历史表中追加新版本记录
3. 告知用户本次优化改了什么、效果如何

### 测试基准

`test-prompts.json` 包含 3 个典型测试用例，覆盖：
1. 基础场景：9张照片每页9张，A4，contain模式
2. 多页场景：15张照片每页4张，150 DPI
3. 特殊参数：cover裁剪填满，每页6张，A3纸

Darwin 优化时必须用这些测试用例做实测验证，确保改动不会破坏已有功能。新增重大功能时，应同步补充对应的测试用例到 `test-prompts.json`。
